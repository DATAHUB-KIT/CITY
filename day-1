response=client.models.generate_content(
    model="gemini-3.5-flash",
    contents="Hello"
)
  ########################################
  
from transformers import pipeline

classifier = pipeline("sentiment-analysis")
classifier("this product is very good ")
############################################################
